# Running Program locally
1. Run npm install to install Node.js dependencies
2. Run npm start to compile CSS and Javascript files and watch for changes.
3. Open http://localhost:3000/ in your browser.

